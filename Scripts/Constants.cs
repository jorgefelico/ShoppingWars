using Godot;

public static class Constants
{
    // Pure vibrant lime/green with 0 blue/red
    public static readonly Color PATROL_GREEN = new Color(0.05f, 1.0f, 0.0f) * 2.5f;

    // Rich warm amber-gold
    public static readonly Color PATROL_YELLOW = new Color(1.0f, 0.4f, 0.0f) * 2.5f;

    // Pure deep neon red (0 green, 0 blue)
    public static readonly Color PATROL_RED = new Color(1.0f, 0.0f, 0.0f) * 3.0f;
}